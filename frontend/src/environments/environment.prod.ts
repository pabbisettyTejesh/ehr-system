// TODO: replace with your deployed backend's public URL once it's hosted
// somewhere reachable from the internet (Render, Railway, EC2, etc).
// localhost:8080 only works when both frontend and backend run on your
// own machine -- it will NOT work from a Vercel-hosted frontend.
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-BACKEND-DOMAIN.com/api'
};
